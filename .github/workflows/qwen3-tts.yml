name: Qwen3 TTS Voice Clone (CPU/GGML)

on:
  workflow_dispatch:
    inputs:
      text:
        description: 'Text to synthesize'
        required: true
        default: 'Hello, this is a test of my cloned voice on GitHub Actions.'
      ref_text:
        description: 'Exact transcript of MyVoice.wav'
        required: true
        default: 'This is the exact transcript of what is said in MyVoice.wav.'
      output_name:
        description: 'Output filename'
        required: false
        default: 'output.wav'

jobs:
  tts-cpu:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        
      - name: Verify MyVoice.wav exists
        run: |
          if [ ! -f "./MyVoice.wav" ]; then
            echo "❌ ERROR: MyVoice.wav not found in root!"
            exit 1
          fi
          echo "✅ MyVoice.wav found"

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      # ✅ FIX: Install libcudart via pip nvidia package (no apt repo needed)
      - name: Install CUDA Runtime Library via pip
        run: |
          pip install nvidia-cuda-runtime-cu12
          # Find and symlink libcudart.so.12 to system path
          LIB_PATH=$(python -c "import nvidia.cuda_runtime; import os; print(os.path.join(os.path.dirname(nvidia.cuda_runtime.__file__), 'lib'))")
          echo "CUDA runtime lib path: $LIB_PATH"
          sudo ln -sf "$LIB_PATH/libcudart.so.12" /usr/lib/x86_64-linux-gnu/libcudart.so.12
          sudo ldconfig
          echo "✅ Verification:"
          ldconfig -p | grep libcudart || echo "Checking alternative..."
          find /usr/lib -name "libcudart*" 2>/dev/null

      - name: Cache pip & HF models
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/pip
            ~/.cache/huggingface
          key: ${{ runner.os }}-qwen3-ggml-v4

      - name: Install faster-qwen3-tts with GGML backend
        run: |
          pip install torch torchaudio --index-url https://download.pytorch.org/whl/cpu
          pip install "faster-qwen3-tts[ggml]"
          faster-qwen3-tts --help

      - name: Run TTS Voice Clone via GGML (CPU fallback)
        env:
          CUDA_VISIBLE_DEVICES: ""
        run: |
          faster-qwen3-tts --backend ggml --quant BF16 clone \
            --model Qwen/Qwen3-TTS-12Hz-0.6B-Base \
            --ref-audio ./MyVoice.wav \
            --ref-text "${{ github.event.inputs.ref_text }}" \
            --text "${{ github.event.inputs.text }}" \
            --language English \
            --output "${{ github.event.inputs.output_name }}"

      - name: Upload Audio Artifact
        uses: actions/upload-artifact@v4
        with:
          name: tts-output
          path: ${{ github.event.inputs.output_name }}
          retention-days: 7
